{
  "rules": {
    ".read": false,
    ".write": false,

    "sellers": {
      "$uid": {
        ".read": "auth != null && auth.uid == $uid",
        ".write": "auth != null && auth.uid == $uid",
        "products": {
          ".read": "auth != null && auth.uid == $uid",
          "$productId": {
            ".read": "auth != null && auth.uid == $uid",
            ".write": "auth != null && auth.uid == $uid"
          }
        }
      }
    },

    "events": {
      "$sellerId": {
        ".read": "auth != null && auth.uid == $sellerId",
        ".write": "auth != null && root.child('publicStores').child($sellerId).exists()",
        "$eventId": {
          ".validate": "newData.hasChildren(['type', 'sellerId', 'timestamp']) && newData.child('type').val().matches(/^(store_view|product_view|wa_click)$/) && newData.child('timestamp').isNumber() && newData.child('sellerId').val() == $sellerId"
        }
      }
    },

    "publicStores": {
      "$sellerId": {
        ".read": true,
        ".write": "auth != null && auth.uid == $sellerId"
      }
    },

    "admin": {
      ".read": "auth != null && root.child('sellers').child(auth.uid).child('isAdmin').val() == true",
      ".write": "auth != null && root.child('sellers').child(auth.uid).child('isAdmin').val() == true"
    }
  }
}