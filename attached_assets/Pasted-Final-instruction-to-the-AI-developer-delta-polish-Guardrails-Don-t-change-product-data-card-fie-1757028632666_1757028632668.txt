Final instruction to the AI developer (delta polish)
Guardrails

Don’t change product data, card fields, or the card layout itself.

Brand spelling is ShopLynk everywhere.

1) Header consistency

Confirm header is sticky + blurred:

class="sticky top-0 z-40 bg-white/80 backdrop-blur supports-[backdrop-filter]:bg-white/60 border-b border-slate-100"


Active tab gets the gradient underline:

{active && <span className="absolute inset-x-2 -bottom-[2px] h-[2px] rounded-full bg-gradient-to-r from-sky-500 to-violet-500" />}


Use one wordmark (no extra glyph). Size ~h-5 md:h-6, keep left padding consistent with page grid.

2) Primary actions + tokens

Make + Add Product use our brand gradient + focus ring (matches landing CTA):

className="rounded-full bg-gradient-to-r from-sky-500 to-violet-500 px-4 py-2 text-white font-semibold shadow-sm hover:shadow-md focus-visible:ring-2 focus-visible:ring-sky-400"


Add (or verify) Tailwind tokens:

// tailwind.config.js
extend: {
  colors: {
    brand: { sky: '#0ea5e9', violet: '#7c3aed' }
  },
  boxShadow: {
    soft: '0 1px 2px rgba(15, 23, 42, .06)',
    lift: '0 6px 20px rgba(15, 23, 42, .08)'
  }
}

3) Card container + hover

Card wrapper:

class="rounded-2xl border border-slate-200 bg-white shadow-soft transition hover:shadow-lift hover:-translate-y-0.5"


Product image has rounded top and lazy loading:

class="rounded-t-2xl"
loading="lazy" decoding="async"

4) Footer alignment across cards

Add a spacer above the button row so different tag counts don’t misalign footers:

<div className="min-h-[64px] md:min-h-[56px]" />

5) Pills + chips

Keep your price chip as-is but ensure the stock pill colors are on brand:

In stock: bg-emerald-50 text-emerald-700

Low stock: bg-amber-50 text-amber-700

Tag pills use soft backgrounds (already good); ensure consistent rounded-full px-2.5 h-7 sizing.

6) A11y + keyboard polish

Every icon button (favorite, ellipsis, copy link, preview) gets aria-label + title.

Search input has aria-label="Search products".

Visible focus on all actionable elements:

class="focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-sky-400 focus-visible:ring-offset-2"

7) Loading + empty states

Add skeletons while products load (single shared skeleton component).

Keep the existing empty state style consistent with landing (soft gradient halo + icon).

8) Responsive checks

Grid: grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-6.

Verify no horizontal scroll on 320–360px devices, and that action buttons wrap cleanly.

9) Visual QA (acceptance)

Sticky blurred header with brand underline on the active tab.

Single ShopLynk wordmark; spacing aligned to page grid.

Gradient CTA on + Add Product; consistent focus ring.

Cards hover-lift uniformly; footers line up; no image edge clipping.

All icons are labeled + keyboard navigable; no focus traps.

that’s it—apply the above and we’re done with polishing.