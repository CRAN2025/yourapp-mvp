# CTA Target Updates: /auth → /app

## Summary
Updated all CTAs (Call-to-Action buttons and links) to use `/app` as the canonical destination instead of `/auth` for unified user flow.

## Files Changed

### client/src/pages/MarketLanding.tsx
```diff
-    const authUrl = APP_ORIGIN ? `${APP_ORIGIN}/auth` : '/auth';
+    const appUrl = APP_ORIGIN ? `${APP_ORIGIN}/app` : '/app';
     const fullUrl = authUrl + `?${qp.toString()}`;
     
     if (APP_ORIGIN) {
       window.location.href = fullUrl;
     } else {
-      navigate(`/auth?${qp.toString()}`);
+      navigate(`/app?${qp.toString()}`);
     }

-    const authUrl = APP_ORIGIN ? `${APP_ORIGIN}/auth` : '/auth';
+    const appUrl = APP_ORIGIN ? `${APP_ORIGIN}/app` : '/app';
     
     if (APP_ORIGIN) {
       window.location.href = appUrl;
     } else {
-      navigate('/auth');
+      navigate('/app');
     }

-                <li><a href="/auth" style={{ color: 'var(--ink)', opacity: 0.7, textDecoration: 'none', fontSize: 14 }}>Create Store</a></li>
+                <li><a href="/app" style={{ color: 'var(--ink)', opacity: 0.7, textDecoration: 'none', fontSize: 14 }}>Create Store</a></li>
```

### client/src/pages/Upgrade.tsx
```diff
-              <Link href="/auth">
+              <Link href="/app">
                 <Button variant="outline" data-testid="button-sign-in">
                   Sign In
                 </Button>
               </Link>

-              <Link href="/auth">
+              <Link href="/app">
                 <Button data-testid="button-get-started">
                   Get Started
                 </Button>
               </Link>

-            <Link href="/auth">
+            <Link href="/app">
               <Button size="lg" variant="secondary" data-testid="button-start-free">
                 Start Free Today
                 <ArrowRight className="w-4 h-4 ml-2" />
               </Button>
             </Link>
```

## Verification Commands
```bash
# Verify no /auth references remain
grep -R --line-number -E 'href="/auth"|to="/auth"|push\("/auth"\)' client/src
# Result: No matches found ✅

# Verify /app references exist
grep -R --line-number -E 'href="/app"|to="/app"|push\("/app"\)' client/src
# Result: 4 matches found in MarketLanding.tsx and Upgrade.tsx ✅
```